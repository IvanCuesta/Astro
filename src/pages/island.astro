---
import InteractiveIsland from '../components/InteractiveIsland.jsx';
import DynamicDataIsland from '../components/DynamicDataIsland.jsx';
---
<html>
<head>
  <title>Astro Island</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 20px;
    }
    h1 {
      color: #007bff;
    }
    .container {
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    .content {
      width: 60%;
      padding: 20px;
      border: 1px solid #ccc;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      margin-bottom: 20px;
    }
    button {
      margin: 5px;
      padding: 10px 20px;
      font-size: 16px;
      background-color: #007bff;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }
    button:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <h1>Ejemplo de Islas de Astro</h1>
  <div class="container">
    <div class="content">
      <h2>Contenido Estático</h2>
      <p>Esta parte de la página se carga rápidamente porque no tiene interactividad.</p>
      <img src="https://via.placeholder.com/400x200" alt="Imagen estática">
    </div>
    <button id="loadIslandButton">Cargar Islas Interactivas</button>
    <div id="interactiveContent" class="content" style="display: none;">
      <!-- Estas islas se cargarán solo cuando el usuario haga clic en el botón -->
      <InteractiveIsland client:load />
      <DynamicDataIsland client:load />
    </div>
  </div>

  <script>
    // Esperar a que el DOM esté completamente cargado
    document.addEventListener('DOMContentLoaded', () => {
      const loadButton = document.getElementById('loadIslandButton');
      const interactiveContent = document.getElementById('interactiveContent');
      
      if (loadButton && interactiveContent) {
        loadButton.addEventListener('click', () => {
          interactiveContent.style.display = 'block';
        });
      }
    });
  </script>
</body>
</html>
